# Validate the "checks" parameter

Validate the "checks" parameter

## Usage

``` r
validateChecks(checks, messageStore)
```

## Arguments

- checks:

  the checks that have been passed to executeChecks

- messageStore:

  checkmate collection
